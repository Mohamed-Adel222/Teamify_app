# Web Framework
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.0.0
Flask-JWT-Extended>=4.6.0
Flask-Bcrypt>=1.0.1
Flask-CORS>=4.0.0
Flask-Limiter>=3.5.0
Flask-Caching>=2.3.0
redis>=5.0.0
Flask-SocketIO>=5.3.0

# Async backend — gevent is fully compatible with Python 3.13.
# eventlet is NOT supported on Python 3.12+ (breaks on stdlib ssl/socket).
# gevent-websocket provides the low-level WebSocket upgrade handler used
# by Flask-SocketIO when async_mode="gevent".
gevent>=24.2.1
gevent-websocket>=0.10.1
flasgger>=0.9.7

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth and Security
python-dotenv>=1.0.0
cryptography>=42.0.0
PyJWT>=2.8.0
google-auth>=2.29.0

# ML Core - required for all pkl models
scikit-learn>=1.4.0
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.2.0

# HTTP Client
requests>=2.31.0
resend>=2.0.0

# Validation
marshmallow>=3.21.0

# Scheduling
APScheduler>=3.10.0

# DistilBERT task category model (model1_category/model.safetensors present)
transformers>=4.40.0
torch>=2.2.0

# Optional: Claude API for mentor report generation
# Uncomment when ANTHROPIC_API_KEY is set in .env
# anthropic>=0.25.0

# Optional: PDF export for CV Builder (POST /api/ai/cv/build returns JSON;
# PDF generation via ai_resume_builder.generate_pdf() requires this)
# reportlab>=4.0.0
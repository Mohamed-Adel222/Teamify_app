# Production deployment requirements — no torch/transformers.
#
# DistilBERT is NOT required in production. POST /api/ai/classify-task and
# deferred task classification use the keyword fallback by design.
# Do not add the full PyTorch package or a CPU wheel here:
#   - Render typical RAM is 512 MB
#   - torch CPU wheels are still hundreds of MB installed
#   - DistilBERT weights are Git LFS (~268 MB) and are not materialized
#     unless `git lfs pull` is run
#   - loading both at gunicorn boot would OOM or miss the deploy timeout
# Opt in only on a large local/dev machine: AI_ENABLE_DISTILBERT=true
# See docs/AI_MODELS.md.

# WSGI server (required for production)
gunicorn>=22.0.0

# Web Framework
Flask>=3.0.0
Flask-SQLAlchemy>=3.1.0
Flask-Migrate>=4.0.0
Flask-JWT-Extended>=4.6.0
Flask-Bcrypt>=1.0.1
Flask-CORS>=4.0.0
Flask-Limiter>=3.5.0
Flask-Caching>=2.3.0
redis>=5.0.0
Flask-SocketIO>=5.3.0

# Async backend — gevent is fully compatible with Python 3.13.
# eventlet is NOT supported on Python 3.12+ (breaks on stdlib ssl/socket).
# gevent-websocket provides the low-level WebSocket upgrade handler used
# by Flask-SocketIO when async_mode="gevent".
gevent>=24.2.1
gevent-websocket>=0.10.1
flasgger>=0.9.7

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.13.0

# Auth and Security
python-dotenv>=1.0.0
cryptography>=42.0.0
PyJWT>=2.8.0
google-auth>=2.29.0

# ML Core - required for pkl models that do NOT need torch
# (scikit-learn models: Delay_Predictor, teamify_model, security_model,
# assignment GradientBoosting, Chat_Summarization). Cap below 2.x because
# the checked-in pickles were trained on sklearn 1.5–1.8.
scikit-learn>=1.5.0,<1.10
joblib>=1.3.0
numpy>=1.26.0
pandas>=2.2.0

# HTTP Client
# PyOpenSSL is required so requests/urllib3 can do HTTPS after
# gevent.monkey.patch_all(ssl=False) (needed for Render Postgres).
requests>=2.31.0
pyopenssl>=24.0.0
resend>=2.0.0

# Transactional email (Resend)
resend>=2.0.0

# Validation
marshmallow>=3.21.0

# Scheduling
APScheduler>=3.10.0

# PDF export for CV Builder
reportlab>=4.0.0

# 2FA / TOTP
pyotp>=2.9.0
qrcode[pil]>=8.0

# NOTE: torch and transformers stay excluded.
# DistilBERT is not a production dependency. The supported path is the
# keyword fallback (AI_ENABLE_DISTILBERT defaults to false).
# sklearn/joblib/pandas above ARE required for the .pkl models.
